#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Print alphabet except q and e'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);

	putchar('\n');

	return (0);
}
